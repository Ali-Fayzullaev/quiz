# 🎯 Quiz Backend API

Мощный backend для Quiz приложения с поддержкой WebSocket, системой рейтингов, геймификацией и соревновательными элементами.

## 🚀 Особенности

### 🎮 Игровые функции
- **Множественные типы вопросов**: одиночный выбор, множественный выбор, текстовые ответы
- **Система рейтингов**: ELO рейтинг, лиги (Бронза → Мастер)
- **Real-time викторины**: живые игры через WebSocket
- **Мультиплеер**: дуэли 1vs1, командные битвы
- **Достижения и бейджи**: 50+ различных достижений

### 🏆 Геймификация
- **Система уровней**: опыт и прогрессия
- **Валюта**: монеты и гемы
- **Инвентарь**: бустеры, аватары, темы
- **Стрики**: система ежедневной активности
- **Лидерборды**: глобальные и локальные рейтинги

### 🌐 Социальные функции
- **Система друзей**: добавление, чат, статус онлайн
- **Вызовы**: дуэли между друзьями
- **Комментарии и рейтинги**: для викторин
- **Лента активности**: достижения друзей

### 🔧 Технические особенности
- **WebSocket**: Socket.io для real-time взаимодействия
- **Кэширование**: Redis для производительности
- **Безопасность**: JWT, bcrypt, rate limiting
- **Логирование**: Winston с ротацией логов
- **Валидация**: Joi схемы
- **Мониторинг**: метрики производительности

## 📦 Установка

### Предварительные требования
- Node.js 18+
- MongoDB 5+
- Redis 6+

### Шаги установки

1. **Клонирование репозитория**
```bash
git clone <your-repo-url>
cd quiz-backend
```

2. **Установка зависимостей**
```bash
npm install
```

3. **Настройка переменных окружения**
```bash
cp .env.example .env
# Отредактируйте .env файл со своими настройками
```

4. **Запуск баз данных** (если локально)
```bash
# MongoDB
mongod --dbpath /data/db

# Redis
redis-server
```

5. **Запуск сервера**
```bash
# Разработка
npm run dev

# Продакшн
npm start
```

## ⚙️ Настройка

### Переменные окружения
```env
# Сервер
PORT=5000
NODE_ENV=development

# База данных
MONGODB_URI=mongodb://localhost:27017/quiz_db
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRE=7d

# Cloudinary (загрузка изображений)
CLOUDINARY_NAME=your_cloudinary_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Email (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password
```

## 🛠️ API Endpoints

### Аутентификация
```
POST /api/auth/register     - Регистрация
POST /api/auth/login        - Авторизация  
POST /api/auth/refresh      - Обновление токенов
POST /api/auth/logout       - Выход
POST /api/auth/forgot-password - Сброс пароля
```

### Пользователи
```
GET  /api/users/:id         - Профиль пользователя
PUT  /api/users/profile     - Обновить профиль
GET  /api/users/search      - Поиск пользователей
GET  /api/users/leaderboard - Глобальный лидерборд
```

### Викторины
```
GET  /api/quizzes           - Список викторин
POST /api/quizzes           - Создать викторину
GET  /api/quizzes/:id       - Получить викторину
PUT  /api/quizzes/:id       - Обновить викторину
GET  /api/quizzes/:id/questions - Получить вопросы
```

### Игра
```
POST /api/game/start/:quizId    - Начать игру
POST /api/game/answer           - Отправить ответ
POST /api/game/complete         - Завершить игру
GET  /api/game/results/:id      - Результаты игры
```

### Мультиплеер
```
POST /api/game/multiplayer/create - Создать комнату
POST /api/game/multiplayer/join   - Присоединиться к комнате
```

## ⚡ WebSocket События

### Клиент → Сервер
```javascript
// Присоединение к викторине
socket.emit('quiz:join', { quizId, sessionId });

// Начало игры
socket.emit('quiz:start', { sessionId });

// Отправка ответа
socket.emit('quiz:answer', { 
  sessionId, 
  questionId, 
  answer, 
  timeSpent 
});

// Создание комнаты
socket.emit('room:create', { 
  quizId, 
  maxPlayers: 4, 
  isPrivate: false 
});

// Вызов на дуэль
socket.emit('challenge:send', { 
  opponentId, 
  quizId, 
  message 
});
```

### Сервер → Клиент
```javascript
// Уведомления о викторине
socket.on('quiz:started', (data) => {});
socket.on('quiz:completed', (data) => {});

// Комнаты и мультиплеер
socket.on('room:created', (room) => {});
socket.on('room:player_joined', (player) => {});

// Социальные события
socket.on('challenge:received', (challenge) => {});
socket.on('friend:status_changed', (status) => {});
socket.on('message:received', (message) => {});
```

## 📊 Структура проекта

```
backend/
├── src/
│   ├── config/          # Конфигурации (DB, Redis, Logger)
│   ├── controllers/     # Контроллеры API
│   ├── middleware/      # Middleware (Auth, Error, Validation)
│   ├── models/          # Mongoose модели
│   ├── routes/          # Express маршруты
│   ├── socket/          # WebSocket обработчики
│   ├── utils/           # Утилиты и валидация
│   └── app.js           # Основной файл приложения
├── uploads/             # Загруженные файлы
├── logs/                # Логи приложения
├── .env                 # Переменные окружения
├── .gitignore
└── package.json
```

## 🧪 Тестирование

```bash
# Запуск тестов
npm test

# Тесты с покрытием
npm run test:coverage

# E2E тесты
npm run test:e2e
```

## 🚀 Деплой

### Docker
```bash
# Сборка образа
docker build -t quiz-backend .

# Запуск контейнера
docker run -p 5000:5000 quiz-backend
```

### PM2 (Production)
```bash
# Установка PM2
npm install -g pm2

# Запуск с PM2
pm2 start src/app.js --name quiz-backend

# Мониторинг
pm2 monit
```

## 📈 Мониторинг и логи

- **Логи**: Сохраняются в папке `logs/`
- **Health check**: `GET /health`
- **Метрики**: Производительность запросов
- **Errors**: Автоматическое логирование ошибок

## 🔒 Безопасность

- JWT токены с автоматическим обновлением
- Хеширование паролей через bcrypt
- Rate limiting по IP и пользователю
- Валидация всех входных данных
- CORS настройки
- Helmet для безопасности заголовков

## 🎯 Roadmap

- [ ] Турнирная система
- [ ] AI рекомендации
- [ ] Интеграция с Telegram Bot
- [ ] Мобильные push уведомления
- [ ] Аналитика и метрики
- [ ] Система модерации контента

## 🤝 Участие в разработке

1. Fork проект
2. Создай feature ветку (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Открой Pull Request

## 📄 Лицензия

MIT License - смотри [LICENSE](LICENSE) файл

## 👥 Команда

- **Разработчик**: Quiz Team
- **Email**: support@quiz-app.com
- **Telegram**: @quiz_support

---

**🚀 Готов к созданию эпических викторин!**